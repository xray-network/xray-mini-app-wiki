import { Button, Alert } from "antd"
import StatsTokenMintParams from "@/components/views/StatsTokenMintParams"
import StatsTokenDistribution from "@/components/views/StatsTokenDistribution"

# XRAY Tokenomics

The XRAY token is the core utility and governance asset of the XRAY/Network ecosystem. Designed to fuel platform operations, incentivize participation, and align community interests, XRAY plays a central role in the network’s economic model. From staking and governance to accessing premium features and services, the token serves as a multi-functional tool that powers both user engagement and ecosystem sustainability.

<Alert
  type="success"
  showIcon
  message="Free Distribution"
  description="There was no presale — only a token giveaway distributed through Stage 1 and Stage 2 participation events, involving over 30,000 wallets and a total delegated value exceeding 300 million ADA (over $500,000,000 at the time)."
/>

## Initial Minting Parameters

<StatsTokenMintParams />

## Distribution Events

<StatsTokenDistribution />
